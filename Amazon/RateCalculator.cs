using System;
using System.Collections.Generic;
using System.Text;

namespace Amazon
{
    class RateCalculator
    {
        internal int Calculate() //internal -- only accessible from the same assembly (Amazon is the assembly, aka dll)
        {
            return 0;
        }
    }
}
